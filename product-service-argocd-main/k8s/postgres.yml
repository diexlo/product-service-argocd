auth:
  postgresPassword: "postgres"
  username: "postgres"
  password: "postgres"
  database: "products"

primary:
  persistence:
    enabled: true
    size: 8Gi

service:
  type: ClusterIP
  port: 5432
